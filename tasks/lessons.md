# Lessons Learned

Patterns captured after corrections. Reviewed at the start of each session.

---

## Structure

- **Docs folders belong inside `Grocery_list/`** — not at the parent `Claude_cursor/` level. CLAUDE.md lives at `Grocery_list/CLAUDE.md`.
- **README.md must always reflect the actual folder structure** — update it whenever files are moved or added.
- **`AI Agents/` is now `ai-agents/`** — lowercase, hyphenated, no spaces. All script src references use `./ai-agents/prompts.js`.

---

<!-- Add new lessons below as they occur -->
Output should be Hindi-friendly / user-facing language